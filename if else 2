#include <stdio.h>
#include <stdlib.h>

int main(){
	
	int x, y, z;
	
	scanf("%d %d", &x, &y);
	
	if(x>y){
		z = x;
		printf("Z = %d\n", z);
	}else if(y>x){
		z = y;
		printf("Z = %d\n", z);
	}else if( x == y){
		z = 0;
		printf("Z = %d\n", z);
	}
		
		
	return 0;
		
}
